# docker-compose.yml
version: "3.8"

services:
  mongo:
    image: mongo:7.0
    restart: unless-stopped
    environment:
      MONGO_INITDB_ROOT_USERNAME: kartikey
      MONGO_INITDB_ROOT_PASSWORD: 123456789
      MONGO_INITDB_DATABASE: appdb
    volumes:
      - mongo-data:/data/db
    ports:
      - "27017:27017"
    healthcheck:
      test: ["CMD", "mongo", "--eval", "db.adminCommand('ping')"]
      interval: 10s
      timeout: 5s
      retries: 5

  backend:
    build:
      context: ./Basic
      dockerfile: Dockerfile
    depends_on:
      - mongo
    environment:
      # connection string your app should read from process.env.MONGO_URI
      MONGO_URI: "mongodb+srv://tiwarikartikey777:HrcvSeaGfbKbh1pI@cluster0.6w2iedb.mongodb.net/"
      PORT: 5000
      NODE_ENV: production
    ports:
      - "5000:5000"
    restart: unless-stopped

volumes:
  mongo-data:

